<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.som.justjava">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/pc"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    <activity android:name=".MainActivity"/>
        <activity android:name=".register"/>
        <activity android:name=".cappuccino"></activity>
        <activity android:name=".espreso"></activity>
        <activity android:name=".caffelat"></activity>
        <activity android:name=".Americano" ></activity>
        <activity android:name=".lait"></activity>
        <activity android:name=".mocaha"></activity>
        <activity android:name=".macho"></activity>
    </application>

</manifest>
